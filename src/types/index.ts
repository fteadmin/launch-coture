export interface ProgramTier {
  tier: number;
  title: string;
  description: string;
}

export interface CoreElement {
  title: string;
  description: string;
}

export interface CommunityAdvantage {
  title: string;
  description: string;
}

export interface ApplicationStep {
  step: string;
  title: string;
  description: string;
}

export interface Statistic {
  value: string;
  label: string;
  description: string;
}